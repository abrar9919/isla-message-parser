export enum MessageSegmentType {
  // Note: It would be good to get the full form of the abbreviations in the keys to make the code easier to read
  Message = "MSG",
  EVT = "EVT",
  PATIENT_INFO = "PRS",
  DETAILS = "DET",
}
